import React from 'react'
import { CircularProgress } from '@mui/material'
const Loading = () => {
  return (
    <main>
      <CircularProgress />
    </main>
  )
}

export default Loading
